import CodeBlock from '@theme/CodeBlock';

<CodeBlock language="javascript" showLineNumbers>
{`import { Text2VideoPipeline } from "openvino-genai-node";

const pipe = await Text2VideoPipeline(model_path, "${props.device || 'CPU'}");
const result = await pipe.generate(prompt);

console.log("Video tensor shape:", result.video.getShape());
`}
</CodeBlock>
