import CodeBlock from '@theme/CodeBlock';

<CodeBlock language="cpp" showLineNumbers>
{`#include "openvino/genai/text2speech_pipeline.hpp"
#include <iostream>

int main() {
    ov::genai::Text2SpeechPipeline pipeline("speecht5_tts", "${props.device || 'CPU'}");
    auto result = pipeline.generate("Hello OpenVINO GenAI");
    std::cout << "Audio samples: " << result.speeches[0].size() << "\n";
}
`}
</CodeBlock>
