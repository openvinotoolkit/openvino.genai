# Optional dependencies for WWB speech-generation evaluation.
# Install these only when running the speech-generation benchmark.

jiwer>=3.0.0,<=4.0.0
faster-whisper>=1.0.0,<=1.2.1
speechbrain>=1.0.0,<=1.0.3
